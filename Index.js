import { View, Text } from 'react-native'
import React from 'react'

const TestTitle = ({title}) => {
  return (
    <View>
      <Text>TestTitle {title}</Text>
    </View>
  )
}

export default TestTitle